# Disassembler

The disassembler uses the Capstone Engine to disassemble code for various architectures. The `disasm.disassemble` function takes a byte string and a starting address as input and prints the disassembly to the console.